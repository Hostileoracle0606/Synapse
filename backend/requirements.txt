certifi
fastapi==0.115.0
firecrawl-py>=1.4.0
google-genai==1.5.0
httpx==0.28.1
python-dotenv==1.0.1
supabase==2.28.3
trafilatura==1.12.0
uvicorn==0.30.0
youtube-transcript-api==1.2.4

# Test-only — kept here so the standard `pip install -r requirements.txt`
# also sets up tests. Move to requirements-dev.txt if you want to keep prod
# images leaner.
pytest==8.3.5
pytest-asyncio==0.24.0
